train_path = "data/balanced/train.simple.json"
eval_path = "data/balanced/dev.simple.json"
track = "simple"
prompt = "simple/zero_shot"

model = "3.0/gpt_bert/instruction_tune"
max_length = 1024
max_new_tokens = 1  # tuning teaches the model to emit the digit first

epochs = 2
lr = 2e-5
train_batch_size = 4

batch_size = 8
device = "cuda"
output_dir = "results"
