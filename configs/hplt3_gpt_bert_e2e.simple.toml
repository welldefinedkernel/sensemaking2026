eval_path = "data/devset/dev.simple.json"
track = "simple"
langs = ["en"]
prompt = "simple/examples"

model = "3.0/gpt_bert/e2e"      
max_length = 16384 

batch_size = 1
device = "mps"
output_dir = "results"
limit = 1
